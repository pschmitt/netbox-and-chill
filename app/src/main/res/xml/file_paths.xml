<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Matches FileDownloadRepository's File(context.cacheDir, "downloads") target dir. -->
    <cache-path name="downloads" path="downloads/" />
    <!-- Durable copies created when attachment sync is enabled. -->
    <files-path name="offline-attachments" path="offline-attachments/" />
</paths>
